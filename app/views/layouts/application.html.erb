<!DOCTYPE html>
<html>
<head>
  <title>TechBaseRails</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<div class="container">
  <nav class="navbar navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand">Navbar</a>
      <% if user_signed_in? %>
        <button class="btn btn-outline-success d-flex"><%= link_to 'Log out', destroy_user_session_path, method: :delete %></button>
      <% else %>
        <button class="btn btn-outline-success d-flex"><%= link_to 'Sign in', new_user_session_path %></button>
      <% end %>
    </div>
  </nav>


  <%= yield %>
  <footer class="fixed-bottom">
    <strong>Hrobovyi Oleksii, 2021</strong>
  </footer>

</div>

</body>
</html>
